---
- hosts: all
  become: true  # Ensures the task runs as root
  become_user: root
  tasks:
    - name: Create a file with content
      copy:
        dest: /root/testfile.txt
        content: "its working"
        owner: root
        group: root
        mode: '0644'
